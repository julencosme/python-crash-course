# Summing 50
#
# Make a list of the numbers from one to fifty, and then use min() and max()
# to make sure your list actually starts at one and ends at fifty.
#
#Also, use the sum() function to add up to fifty

numbers = list(range(1, 51))
min(numbers)
max(numbers)
sum(numbers)

